name: CI

on:
  push:
    branches: [ "main" ]
    paths:
      - "pom.xml"
      - "src/**"
      - ".github/workflows/ci.yml"
      - "Dockerfile"
      - ".dockerignore"
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

# Verhindert parallele doppelte Runs bei schnellen Pushes
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ====================================================
  # Build & Test – Matrix-Builds mit Maven & Surefire
  # ====================================================
  build-test:
    name: Build & Test (${{ matrix.os }} / Java ${{ matrix.java }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        java: [17, 21]
        exclude:
          - os: windows-latest
            java: 21  # Kombination ausschließen

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Temurin ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
          cache: maven

      - name: Build & Test (JaCoCo + Surefire)
        run: mvn -B -DskipTests=false clean verify

      - name: Upload Surefire reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-${{ matrix.os }}-java${{ matrix.java }}
          path: "**/target/surefire-reports/**"
          retention-days: 7

      - name: Upload JaCoCo XML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-${{ matrix.os }}-java${{ matrix.java }}
          path: "**/target/site/jacoco/jacoco.xml"
          retention-days: 7

  # ====================================================
  # SonarCloud – Qualitätsanalyse & Coverage
  # ====================================================
  sonar:
    name: SonarCloud Analysis (Ubuntu / Java 21)
    runs-on: ubuntu-latest
    needs: build-test
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Build for SonarCloud
        run: mvn -B -DskipTests=false clean verify

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=CStallinger_cicd-BA-uebung01-Stallinger
            -Dsonar.organization=cstallinger
            -Dsonar.java.binaries=target/classes
            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml

  # ====================================================
  # Docker – Image-Build, Security Scans & Artifact Export
  # ====================================================
  docker:
    name: Build Docker Image & Security Scans
    runs-on: ubuntu-latest
    needs: sonar   # läuft erst nach erfolgreichem Sonar-Job
    if: github.ref == 'refs/heads/main'
    concurrency:
      group: docker-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK (Cache für Maven-Stage)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Compute short SHA
        id: vars
        run: echo "short_sha=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      # Build das Image und lädt es in die lokale Engine
      - name: Build Docker Image (local/app:<sha> und local/app:vuln)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          load: true
          tags: |
            local/app:${{ steps.vars.outputs.short_sha }}
            local/app:vuln

      # Exportiere Image als TAR-Datei (für andere Übungen)
      - name: Save Docker image as .tar
        run: docker save -o image-${{ steps.vars.outputs.short_sha }}.tar local/app:${{ steps.vars.outputs.short_sha }}

      - name: Upload Docker Artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image-${{ steps.vars.outputs.short_sha }}
          path: image-${{ steps.vars.outputs.short_sha }}.tar
          retention-days: 7

      # ----------------- GRYPE: Security Scan -----------------
      - name: Install Grype
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin
          grype version

      # "Report only" – bricht den Build NICHT ab (für Basislauf / Screenshots)
      - name: Scan image with Grype (JSON)
        run: |
          grype local/app:${{ steps.vars.outputs.short_sha }} -o json > grype-report.json || true

      - name: Upload Grype JSON report
        uses: actions/upload-artifact@v4
        with:
          name: grype-report-${{ steps.vars.outputs.short_sha }}
          path: grype-report.json
          retention-days: 7

      # ----------------- TRIVY: Security Scan -----------------
      - name: Install Trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh \
            | sudo sh -s -- -b /usr/local/bin
          trivy --version

      # "Report only" – bricht den Build NICHT ab (Basisvariante)
      - name: Scan image with Trivy (JSON)
        run: |
          trivy image --format json --output trivy-report.json local/app:${{ steps.vars.outputs.short_sha }} || true

      - name: Upload Trivy JSON report
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report-${{ steps.vars.outputs.short_sha }}
          path: trivy-report.json
          retention-days: 7
